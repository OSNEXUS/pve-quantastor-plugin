# Upstream PVE source (reference only, not part of the plugin)
pve-upstream/

# Debian build artifacts (generated by dpkg-buildpackage / dh)
debian/debhelper-build-stamp
debian/files
debian/*.substvars
debian/pve-storage-quantastor/
debian/.debhelper/

# Built deb staged at repo root for upload to GitHub Releases — never commit
*.deb
*.buildinfo
*.changes

# Legacy patch source directories (untracked reference files)
patches/**/src/

# Developer working notes (not part of the public release)
DEVNOTES.md
